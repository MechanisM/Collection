	
	/**
	 * Выполнение глубоких итераций (с учётом контекста)
	 * 
	 * @this {Colletion Object}
	 * @param {String} children - контекст для шага вниз
	 * @param {String} action - имя операции
	 * @param {Array} param - массив входных параметров
	 * @return {mixed}
	 */
	$.Collection.fn.deep = function (children, action, param) {
		
	};